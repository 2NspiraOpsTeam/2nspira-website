# Stripe test-mode secret key (replace with real key for production)
STRIPE_SECRET_KEY=sk_test_placeholder_use_real_key

# Portal backend URL (downstream client-portal-v1 instance)
PORTAL_URL=http://localhost:3001
